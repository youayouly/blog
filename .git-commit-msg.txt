feat(home/about): redesign / and /about hub with interactive China map, mini ProfileCard, restored hero

Home (/):
- Restore Hope home structure into v2 layout (mini ProfileCard + intro card + recommendations + AboutTimeline) bottom-aligned via avatar resize.
- ProfileCard mini: brand-color round buttons (rose email, dark GitHub, WeChat green), suppress Hope external-link arrow, fix WeChat hover QR popup width/sizing.
- Default avatar swap to /gallery/avatar-luke-2026.png; added to avatarPref choices.

About (/about):
- New AboutMePage component (page-about-me class) inspired by ImCao layout, paired with ChinaVisitedMap.
- Left sidebar: NoticeCard + SiteStatsCard (no profile / categories / wechat card).
- Right main panel: refined intro (02 hou, computer science, HKU), 'qu guo de di fang' interactive ChinaVisitedMap, learning, recent posts, contact (with hover/click QR popup for `youayouly1`, opens detached window), 'ben zhan gou jian' (NameSilo / Steam Wallpaper Engine / pixiv unnamed / Geist Variable / Maple Mono).
- Hide native Hope page-title via :has(.lk-aboutme); widen container to 1200px to match article hub.
- All links text-decoration:none, hover/focus uses color shift.

ChinaVisitedMap:
- 34-province grid with light blue/purple highlight per visited region.
- Visited list adds Shanghai + Fujian on top of Beijing / Zhejiang / Hunan / Shaanxi / Guangdong / Hong Kong.
- Left interactive city cards: live local time (Intl) and weather (wttr.in lazy fetch), click syncs with map highlight.

Assets:
- Save new avatar avatar-luke-2026.png (~47KB) and cropped wechat-qr.png/.jpg (mid-only QR).

Footer and global SCSS:
- Footer breakout uses 100vw with overflow-x clip on root to avoid horizontal overflow caused by classic scrollbar.
- Various index.scss patches: page-home-hub transparency, .lk-card--mini sizing locks (220px / content-height), .lk-aboutme links no underline, China map cards/legend styling.
